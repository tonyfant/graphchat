# Retrieves weather forecast by address. Requires a address search string like city and state
query getWeatherData($search: String!) {
    geoByAddress(search: $search) {
        lat
        long
        weather {
          conditions
          forecastSnowFall
          temperature
          windSpeed
          currentSnowDepth
        }
    }
}

